
Es posible usar referencias relativas al grabar macros que no dependan de unacelda fija.


Algunas macros se pueden grabar en EXCEL mismo. 
Al escoger la opción Libro de macros personal. Será accesible desde todos los lbros de excel.!!


Confiar en el modleo de objetos:
Este parámetro es para los desarrolladores y sirve para controlar o autorizar el acceso por programa 
al modelo de objetos VBA (formularios, módulos y módulos de clase)


Habilitar contenido vuelve  a parecer //Un  documento  se  considera  confiable  para  una  determinada  ubicación:  si  cambia  la  ubicación  de  un  documento 
aprobado, volverá a aparecer la advertencia de seguridad la próxima vez que lo abra.

Es posible activar las macros en una ubicación dada de confinaza para que los libros en ella se consideren confiables.
Ubicaciones de confianza/Agregar nueva ubicación/

Selfcert.exe me permite crear mis propios certificados, es uan herramienta con la que viene excel 2016.

Aquí estan los centros de distribuciín de certificados para distribuir una macros a gran nivel
https://docs.microsoft.com/en-us/previous-versions/ms995347(v=msdn.10)



////

Subrutinas y funciones.


Subrutinas y funciones son procedimientos, un conjunto complejo de isntrucciones que se realiza de forma más breve.
Que a su vez se guadan en módulos,  y/o objetos del libro de excel.

Las funciones se diferencian en que arrojan y/o devuelven un valor Y pueden usarse como Funciones nativas de eEXCEL.
(Idea: Clculos estadísticos y matemática química, un ExcelChem, funciones que arrojen un gráfico deseado de curva de calibración, titulación... blabla).

Existen dos tipos de procedimeintos Sub

Los asociados a procedimientos generales y los asociados a eventos. Open, Close, Sve del objeto Workbook. Y asi mismo cada objeto fijo en excel posee una cantidad de eventos disoponibles.

Es decir acciones que ocurren porque se da a lugar movientos por el usuario o po rel sistema mismo.


Procedimientos Function.


Devuelven un valor, es excatmente lo mismo que las funciones que usa excel. de fecha, día(), suma(). 





//DECLARACIÓN

Determinar el alcance de un rpocedimeinto.
Nombre +Sub o Function_Lista de argumentos
Si es function se decalra el nombre+ el tipo de valor qeu va a devolver.
Exit para salir del procedimiento
End SUB/FUNCTION para terminar-

ALCANCE.
Existe Public, Private, Static.

Un procedimeinto es publico y quiere decir que se pueden usar desde todos los móduclos de Excel.
Privado solo cuando puede llamarse dentro del mismo procedimiento.
Static, indica que las variables locales del procedimiento se mantienen entre una llamada y otra.

Todos los procedimientos al no indicarse si es Public, private o Friend? son public por defecto.


ARGUMENTOS DE LOS PROCEDIMIENTOS.

Los argumentos se usan para transferir a los procedimeintos parámetros en forma de datos,  puede variar de 0 a varios.

Es como si fuera un delimitador de  los procedimientos, es decir, el tipo de procedimeinto que se usa va ser tomado como :
A opcional, B como valor ,C como referencia
D, como arreglo
E Como variable
F Como tipo, boleano, Byte, Integer, Longo.

Los argumentos tranferidos a procedimeintos son más legibles si  se usa el nombre del argumento
Sintaxis, debido a que como no se puede leer el orden después de una ,  no lo reconoce, es mejor especificar de quien em está hablando de  durante la escritura de la sintaxis.


Nombreargumento := Valor.

Por ejemplo.
If MsgBox(Prompt:= "¿Desea salir de la aplicación?", _  
   Title:="Gestión de ventas", _  ///Al especificar que es el título se gestiona mejor.
   Buttons:=vbYesNo + vbQuestion) = vbYes Then  
   Application.Quit  
End If

Los argumento opcionales aparecen entre corchetes.



LLAMAR A UN PROCEDIMEINTO.

Para llamar se usa la palabra clave Call + NombreProcedimeinto  y ( Lista de argumentos)

Los resultados de funciones se pueden guardar en variables así:

<variable> =Nombre procedimiento ( Lista de argumentos)


Para llamar de un módulo los procedimeintos.

NombreModulo.Procedimeinto.
//thisWorbook.NombreProcedimiento.


Para llamar desde otro Libro se llma así
Application.Run ""NombreLibro!Thiswprokbook.Procedimeinto"
El libro del que se llama debe estar abierto para poder ejectuar la llamada.


10. LLAMAR UNA FINCIÓN VBA DESDE UNA FÓRMULA EN EXCEL.

Todas las funcioens publicas están en el asistente de funciones de Excel. ( Funciones definidas pro usuario)

Puedo añadirle el cuadro de ayuda pero todo esto se hace medianto los argumentos en la función, es decir, los cadros de texto donde ingreso las funciones o argumentos de mi función deben ser explícitos para mejorar la forma en que se ingresan los valores d emi función.


11. VARIABLES

Permiten almacenar valores intermedios durante la ejecución del código VBA para usarlos luego en cálculos, comparaciones, pruebas.
Los tipos de variable dependen de lso tipos de datos, y están limitadas por un tamaño de memoria en bytes.


Número.
Estan en orden de ocupar espacios de memoria.
19:06 12/03/2019
Byte, integer, long, Simple o Float Double Currency Decimal.

Cadena caraceres.

String fija o variable.

String peude contener 2 millones de caracteres.
String fija es As String * Número de caracteres a fijar

Boleana Tipo True o False. Ocupa 2 bytes

Fehca
 Puede toamr valores de fecha y de hora desde el 1 de enero del año 100 al 31 de diciembre de 9999, ocupa 8 bytes.

Variant

Pueden contener datos de todo tipo, a demás de empty, null, error.
Puede usarse para tratatr numeros y caracteres, sn embargo el consumo de memoria se ve ampliado, consume 16 bytes apara numeros 22 para caracteres.
Se ve reflejado en módulos complejos.


OBJETO

Tipo es Object, Se crea una variable que contenga un objeto.
Los objetos deben declararse al referirse a ellos.

Mediante As Object, As Woorkbook, As chart.
 Es decir, darle sun nombre.

Por ejmplo, para asinarle una variable a un objeto.

Dim Hoja As WorkSheet
Dim Grafico As Chart

Para asignar un Objeto a una variable se usa 

Dim Rango As Range
Set Rango = Range ("")
Set Rango =Nothing / Para desactivar la asociación entre una variable y un objeto determinado.

Vamso en la página 40 Del libro






PILDORAS INFROMÁTICAS///


VIDEO 2.

Me hablan de la programación orientada a objetos.

La jerarquía de objetos pueden ser en tipo matriosca.
Application/Worbook/WorkSheet/Char=Range/PivotTables=Names.



Colecciones o Grupos de objetos de la misma clase.

Objetos ismilares pertenen a un grupo de objeto o colección

Colección Sheets, o Workshet. ( Son diferetnes)


Casos para las escepciones de la jerarquia de ojetos para no tener que llamar desde application.ThisWorkbook. bla.bla.
Esas excepcioens depende de como se esté trabajando y a donde se estén haciendo referencias los módulos.
Es decir, Application siempre será excel a menos que se especifique lo contrario.

Workbook hace referencia al libro de trabajo que se trabaja al nombre.
si no se ponen en el código se entiende que es con el libro activo.

Se pueden obviar del código los objetos activos para escribir procedimientos más eficientes.

Inclusive se puede ir más allá, lso objetos tiene por defecto algunas propiedades determinadas que no necesitan llmarse, como propiedades principales. Por ejemplo, la celda / rango, tiene como propiedad principal su valor, de forma que yo puedo escribir así

Rang("A1")=100, SIN NECEDIDAD DE LLAMAR LA PROPIEDAD VALUE. Porque entiende que se está hablando del libro activo la hja activa.


Los métoso y propiedades e diferencian en VBA por dos simbolos despues del punto de jerarqui.Al desplegarse el cuadro se muestra que los metodos tienen unafigura verde, y las propiedades una manito con una especie de sobre.

Tareas parecidas/En un mismo módulo
Tareas que no se relacionan en otro Modulo.


Se pueden resumir pasos al tener métodos similares.
Range y Selection poseene el mismo étodo ClearContents

Para darle color se le puede decir mediante .color= numeor, o vbYellow nombre del color en igles o RGB la mescla ( RGB(num, num, num)
valores entre 0 y 255. para RGB

mÉTODO CLEAR BORRA TODO, INFOMRACIÓN Y CONTENIDO.


https://www.youtube.com/watch?v=NEQEzWtSwzk




///////////////NOTAS:

Necesito realizar  un repaso sobre lo que son las referencias asolutas y relativas para excel.

* El centro de condfainza en desarrolladodr puedo deshabilitar las peticiones de uso de las macros, así como habitliar una foirma digital certificada.


Cualquiera que sea la opción elegida, si instala un programa antivirus compatible con Microsoft Office System 2016, 
los libros que contengan macros serán analizados antes de abrirse.



/// Ideas para aplicativos posteries.

Se puede modificar la cinta de opcines para que parezca más profesional.
Se puede modificar desde  personalización de cinta de opciones, crear una pestaña, añadir subgropos y inclusive ingresar la macros. Por ejmeplo Macro reporte y permita arrojar un reporte.
O correo.

?¿Es `psoble modificar los iconos para la cinta de opciones?

Inclusive se pueden modificar lso iconos de la parte superior del archoo de excel. con macros!!!!


En Ver los exploradores de objetos me permiten ver todos los objetos y suspropiedasdes y métodos.


Los proceso sa nivel de superficie de excel son muy lentos.
Escribe en el disco duro lento
Escribir a nivel de ram, e smucho más rapido.
Má eficiente.


Forma optima de hacer referencia a una hoja.
Mediante el codeName, es decir, puede cmabiarse el nombre de las pestañas.
La sintaxis es más sencilla, se ahorran parentesis.
ctrl + barrras espaceadora muestra la lsita de concidencias.


Revsiar lso conceptos de cómo excel toma los valores de fecha. 


///////////////Preguntas//////////////


Que son procedimeintos Property en excel?

Qué es más rápdo en cuanto a memoria. Las funciones de excel nativas? O las funciones creadas por el usuario.

Puedo hacer un módulo con unprocedimiento donde me refiera a un evento de un objeto del libro? por ejemplo el evento Change en Thsiworkbook, escrito como un procedimiento en un módulo?

///pARECE QU ENO ME PEUDO REFERIR A EVENTOS FUERA DEL DOMINIO de los objetos.
Pero si me puedo referir dentro de un evento a un procedimiento creado en un módulo.


