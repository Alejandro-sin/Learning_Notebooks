crear rama. 

git branch dashboard
git checkout dashboard  me mete en la rama.
git chekout -b nombre_carpeta   me crea la rama y se va  a esa pagian.


cambiar o renombrar

git branch -m nombre_carpeta nombre_carpetanueva.
git branch -d nombre, puedo borrar la rama.





Este proyecto tiene como propósito realizar implementar lo aprendido en el programa Platzi Master  y resolver un conjunto de requisitos del proyecto "searching_roomie" los requisitos fueron los siguientes:

*Lista...


Tecnologías usadas:
*Lista....



(?Github)
Fork
git clone + url
ls
cd

Antes
para ubicarme en rama.

git branch -a
git checkout + name_branch
git branch (verde)
git checkout



Durante:
Trabajo subo

git add .
git commit -m 
git push origin master
